// eslint-disable-next-line quotes
"use strict";

// eslint-disable-next-line no-unused-vars
function rem(pixels) {
  // eslint-disable-next-line no-undef
  return (pixels / $defaultFontSize) * 1;
}

/* ejemplo de uso de esta función:

p {
    font-size: rem(18);
}

*/
